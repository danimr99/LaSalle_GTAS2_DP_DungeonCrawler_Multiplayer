package common;

/**
 * A collection of networking-related constants in the shared (common) package
 * This package could also define Model classes to be shared via ObjectOutputStream / ObjectInputStream + serialization
 */
public class NetworkConstants {
    // IP and port to use, for ease of configuration
    public static final String IP = "localhost";
    public static final int PORT = 50000;
}
